1. When the user explicitly requests an explanation, focus exclusively on explaining the topic. The explanation must be detailed enough to cover the subject clearly, concise enough to avoid unnecessary information, and written in simple, accessible language. Do not include code, implementation steps, alternative solutions, or technical jargon unless the user specifically asks for them.
2. Before executing the task, always review the project's available SKILLs, analyze them carefully, and determine which ones are required for the request. If relevant SKILLs exist, use them and mention which ones were used. If no SKILLs are available or none are relevant, continue with the task execution without mentioning it.
3. For every user request, first produce a detailed and specific execution plan before taking any action.
4. Do not include comments inside any code you write.
5. Whenever you make a change or adjustment to a project, you must locate the **`spec`** folder within that same project and open the **`changelog.md`** file. The **`spec`** folder must always be located inside the project that was modified. In **`changelog.md`**, record the changes in a clear and concise manner, specifying what was changed, which file was modified, and the user request or requirement that prompted the change.